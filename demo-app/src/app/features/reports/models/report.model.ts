export interface UploadTrendEntry  { categoryName: string; month: string; count: number; }
export interface CategoryCountEntry { categoryName: string; count: number; }
export interface StorageEntry       { categoryName: string; totalBytes: number; }
export interface RoleDistributionEntry { roleName: string; userCount: number; }
